import {css} from 'lit';

export const styles = css`
  :host {
    display: block;
    padding: 20px;
    max-width: 60%;
    margin: 0 auto;
    background: var(--var-background-primary);
    color: var(--var-foreground-primary);
  }
`;
